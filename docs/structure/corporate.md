# Corporate structure — local copy pointer

This project uses the **`corporate`** skill for its org ladder, blocker/handoff
rules, and doc layout. Attach that skill when orchestrating.

This file is an optional local copy slot. If your tool can't load the skill
directly in every session, paste the skill's `SKILL.md` body here so agents can
read the rules from the repo. Otherwise leave this pointer as-is — the skill is
the source of truth and this file just marks where a local copy would live.
