# Executive role — redirect

The executive role for **this project** is defined by its bootstrap doc, not
here. Start there:

→ **`docs/agents/executive/CEO.md`**

That file carries the domain-specific charter, read order, task sizing, locked
architecture, and verification command. The generic executive rules (scope,
blockers, handoff, write access) live in the `corporate` skill — attach it
alongside the bootstrap.
